void anyfunction(void) {
	system(”someCommand”);
}
intmain(intargc, char** argv) {
	char* ptr;
	chararray[8];
	ptr = array;
	strcpy(ptr, argv[1]);
	printf(”Arrayhas % sat % pnn”, ptr, &ptr);
	strcpy(ptr, argv[2]);
	printf(”Arrayhas % sat % pnn”, ptr, &ptr);
}